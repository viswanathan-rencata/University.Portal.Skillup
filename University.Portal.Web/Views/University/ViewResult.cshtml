@using University.Portal.Data.Data.ViewModels;
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IEnumerable<ViewExamResultModel>;

@{
    if (Model != null && Model.Count() > 0)
    {
        <table id="table1" class="table table-bordered" style="margin-top:50px" data-filter-control="true">
            <thead>
                <tr>
                    <th data-field="Student Name" data-filter-control="input">Student Name</th>
                    <th data-field="Class" data-filter-control="input">Class</th>
                    <th data-field="Department" data-filter-control="input">Department</th>
                    <th data-field="Subject Code" data-filter-control="input">Subject Code</th>
                    <th data-field="Subject Name" data-filter-control="input">Subject Name</th>
                    <th data-field="Mark" data-filter-control="input">Mark</th>
                    <th data-field="Result" data-filter-control="input">Result</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.StudentName</th>
                        <td>@item.Class</td>
                        <td>@item.Department</td>
                        <td>@item.SubjectCode</td>
                        <td>@item.SubjectName</td>
                        <td>@item.Mark</td>
                        <td>@item.Result</td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <table class="table table-bordered" style="margin-top:50px">
            <thead>
                <tr>
                    <th>Student Name</th>
                    <th>Class</th>
                    <th>Department</th>
                    <th>Subject Code</th>
                    <th>Subject Name</th>
                    <th>Mark</th>
                    <th>Result</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td colspan="7" class="text-center">No Records Found!</td>
                </tr>
            </tbody>
        </table>
    }
}

<div style="float:right">
    <h3>Total Records (@Model.Count())</h3>    
</div>

<script>
    $(function () {
        $('#table1').bootstrapTable();
        $('.fixed-table-loading').remove();
    })
</script>